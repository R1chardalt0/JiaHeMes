<UserControl x:Class="ChargePadLine.Client.CustomerControl.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChargePadLine.Client.CustomerControl"
             mc:Ignorable="d" 
            >
    <UserControl.Resources>
        <Style x:Key="FICON" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="../font/#iconfont"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment"  Value="Center"/>
        </Style>
    </UserControl.Resources>
    <Border Background="#8470FF" Height="30" Padding="0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" />
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBox Grid.Column="1" Style="{StaticResource FICON}" Text="&#xe6fb;"  Foreground="White" HorizontalAlignment="Right"
                                        Margin="0,0,0,0" FontSize="14" Width="200" />
                <TextBox Grid.Column="1"  Text="Title" Foreground="White" Background="Transparent" HorizontalAlignment="Left"
                     Margin="0,0,10,0" FontSize="14" BorderThickness="0" CaretBrush="White" Width="200" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
